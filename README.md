When you clone the repository, do the following:
- cd into dbd-winstreak-tracker/Build
- Type: sudo cp tracker /user/local/bin

Now you can simply type tracker in any directory and it will allow you to use the program.
